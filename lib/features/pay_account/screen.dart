import 'package:flutter/material.dart';

class AddPayAccountScreen extends StatelessWidget {
  const AddPayAccountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Add Pay Account Screen'));
  }
}
